spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/musicStream
    username: postgres
    password: abc
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
